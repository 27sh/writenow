<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Write Now</title>
    <script src="/js/main.js" defer></script>
</head>
<style>
    @font-face {
        font-family: 'GowunBatang-Regular';
        src: url('https://fastly.jsdelivr.net/gh/projectnoonnu/noonfonts_2108@1.1/GowunBatang-Regular.woff') format('woff');
        font-weight: normal;
        font-style: normal;
    }

    body {
        width: 1440px;
        background-color: #D6CFC5;
        margin: 0 auto;
        font-family: 'GowunBatang-Regular';
        position: relative;
    }

    .top-line {
        position: absolute;
        top: 0;
        left: 0;
        width: 500px;
        height: 500px;
        background-image: url('/img/top-line.png');
        background-repeat: no-repeat;
        background-size: contain;
        z-index: 10;
    }


    .pencil-img {
        position: absolute;
        top: 100px;
        right: 100px;
        width: 50px;
        height: 650px;
        background-image: url('/img/pencil.png');
        background-repeat: no-repeat;
        background-size: contain;
        z-index: 1;
        transform: rotate(10deg);
    }

    .mp3-img {
        position: absolute;
        bottom: 0;
        left: 20px;
        width: 350px;
        height: 550px;
        background-image: url('/img/mp3.png');
        background-repeat: no-repeat;
        background-size: contain;
        transform: rotate(-15deg);
    }

    #main-container {
        width: 850px;
        height: 1024px;
        background-color: #F7F3F0;
        margin: 0 auto;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    #container-inline {
        width: 788px;
        height: 970px;
        border: solid 1px #6B5858;
    }

    #title {
        width: fit-content;
        font-size: 64px;
        font-weight: bolder;
        color: #6B5858;
        border-bottom-width: 7px;
        border-bottom-style: double;
        border-bottom-color: #6B5858;
        padding-bottom: 5px;
        margin: 100px auto 350px;
    }

    .text {
        width: 250px;
        font-size: 32px;
        font-weight: normal;
        color: #6B5858;
        border-bottom-width: 1px;
        border-bottom-style: solid;
        border-bottom-color: #6B5858;
        padding-bottom: 5px;
        margin: 0 auto;
        text-align: center;
        padding: 15px;
    }

    #sign-in:hover,
    #login:hover {
        cursor: pointer;
    }


</style>

<body>
<div class="top-line"></div>
<div class="mp3-img"></div>
<div class="pencil-img"></div>

<div id="main-container">
    <div id="container-inline">
        <div id="title">Write Now</div>
        <div id="select-box">
            <div id="sign-in" class="text">회원가입</div>
            <div id="login" class="text">로그인</div>
        </div>
    </div>
</div>

</body>
</html>