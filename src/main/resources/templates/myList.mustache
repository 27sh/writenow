<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Write Now</title>
    <script src="/js/myList.js" defer></script>
    <style>
        @font-face {
            font-family: 'GowunBatang-Regular';
            src: url('https://fastly.jsdelivr.net/gh/projectnoonnu/noonfonts_2108@1.1/GowunBatang-Regular.woff') format('woff');
            font-weight: normal;
            font-style: normal;
        }

        body {
            background-color: #D6CFC5;
            margin: 0;
            font-family: 'GowunBatang-Regular';
            overflow-x: hidden;
        }

        .top-line {
            position: absolute;
            top: 0;
            left: 0;
            width: 500px;
            height: 500px;
            background-image: url('/img/top-line.png');
            background-repeat: no-repeat;
            background-size: contain;
            z-index: 10;
        }

        .mp3-img {
            position: absolute;
            bottom: 0;
            left: calc(50% - 950px / 2 - 220px);
            width: 350px;
            height: 550px;
            background-image: url('/img/mp3.png');
            background-repeat: no-repeat;
            background-size: contain;
            transform: rotate(-15deg);
            z-index: 10;
        }

        .pencil-img {
            position: absolute;
            top: 100px;
            left: calc(50% + 850px/2 + 80px);
            width: 50px;
            height: 650px;
            background-image: url('/img/pencil.png');
            background-repeat: no-repeat;
            background-size: contain;
            z-index: 1;
            transform: rotate(10deg);
        }

        #main-container {
            width: 850px;
            height: 1024px;
            background-color: #F7F3F0;
            margin: 0 auto;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
        }

        #container-inline {
            width: 788px;
            height: 970px;
            border: solid 1px #6B5858;
            position: relative;
        }

        #user-info {
            position: absolute;
            top: 30px;
            right: 30px;
            color: #6B5858;
            font-size: 16px;
            text-align: right;
        }

        #user-info a {
            color: #6B5858;
            text-decoration: underline;
        }

        #title {
            width: fit-content;
            font-size: 64px;
            font-weight: bolder;
            color: #6B5858;
            border-bottom: double 7px #6B5858;
            padding-bottom: 5px;
            margin: 100px auto 30px;
        }

        #write-btn {
            font-size: 18px;
            color: #6B5858;
            cursor: pointer;
            margin: 0 30px 30px auto;
            text-align: right;
            width: fit-content;
            transition: color 0.3s ease;
            position: relative;
        }

        #write-btn::before {
            content: "+ ";
            font-weight: bold;
            color: #6B5858;
        }

        #write-btn:hover {
            color: #5a4a4a;
        }

        #list-container {
            height: 70%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            gap: 10px;
        }

        .post {
            display: flex;
            flex-direction: row;
            color: #6B5858;
            font-size: 16px;
            line-height: 1.4;
            align-items: center;
            border-bottom: 1px solid #6B5858;
            width: 730px;
            height: 40px;
            justify-content: space-between;
        }

        .post-title {
            flex-grow: 1;
            text-align: left;
        }

        .post-date {
            width: 100px;
            text-align: right;
        }

        a.post {
            text-decoration: none;
        }
    </style>
</head>
<body>
<div class="top-line"></div>
<div class="mp3-img"></div>
<div class="pencil-img"></div>

<div id="main-container">
    <div id="container-inline">
        <div id="user-info">
            <div>반갑습니다, <strong>{{member.name}}</strong>님</div>
            <div><a href="/logout">로그아웃</a></div>
        </div>

        <div id="title">나의 일기장</div>

        <div id="write-btn">
            <a href="/diary/write" class="btn-write">글쓰기</a>
        </div>

        <div id="list-container">
            {{#diaries}}
                <a href="/diary/{{dno}}" class="post">
                    <div class="post-title">{{dtitle}}</div>
                    <div class="post-date">{{date}}</div>
                </a>
            {{/diaries}}
        </div>
    </div>
</div>
</body>
</html>
