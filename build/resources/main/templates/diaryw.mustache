<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Write Now</title>
    <link rel="stylesheet" href="/css/diary.css">
    <script src="/js/diaryw.js" defer></script>
    <style>
        #title-input {
            width: 100%;
            font-size: 40px;
            font-weight: bold;
            border: none;
            border-bottom: 3px solid #6B5858;
            text-align: center;
            margin-bottom: 30px;
            background-color: transparent;
            color: #6B5858;
            outline: none;
            font-family: 'GowunBatang-Regular';
        }

        #title-input::placeholder,
        #input-text::placeholder {
            color: #aaa;
        }
        .emoji {
            font-size: 30px;
            cursor: pointer;
            opacity: 1; /* 초기 상태는 불투명 */
            transition: opacity 0.2s ease;
        }

        .emoji.dimmed {
            opacity: 0.5;
        }
        .button-wrapper {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        #finsh-btn {
            width: 100px;
            height: 50px;
            background-color: #D6CFC5;
            font-size: 14px;
            color: #6B5858;
            border: none;
            border-radius: 5px;
        }

    </style>
</head>
<body>
<div class="top-line"></div>
<div class="mp3-img"></div>
<div class="note-img">
    <div class="note-title">오늘의 기분</div>
    <div class="note-options">
        <span class="emoji">😆</span>
        <span class="emoji">😐</span>
        <span class="emoji">😴</span>
    </div>
    <div class="note-title" style="margin-top: 30px;">오늘의 날씨</div>
    <div class="note-options">
        <span class="emoji">☀️</span>
        <span class="emoji">☁️</span>
        <span class="emoji">🌧️</span>
    </div>
</div>

<div id="main-container">
    <div id="container-inline">
        <input id="title-input" type="text" placeholder="제목을 입력하세요" />

        <textarea id="input-text" placeholder="글 내용을 작성해주세요"></textarea>

        <div id="diary-date"></div>

        <div class="button-wrapper">
            <button id="finsh-btn">작성 완료</button>
        </div>

    </div>
</div>
</body>
</html>